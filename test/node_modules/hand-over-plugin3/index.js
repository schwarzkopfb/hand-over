'use strict'

module.exports = MyPlugin

var inherits = require('util').inherits,
    Plugin   = require('../../plugin')

function MyPlugin(opts) {
    Plugin.call(this, 'test3', null, opts)
}

inherits(MyPlugin, Plugin)
